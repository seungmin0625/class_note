# CLAUDE.md

이 파일은 이 저장소에서 작업하는 Claude Code(및 향후 협업자)를 위한 안내 문서입니다.
프로젝트의 상세 요구사항은 [PRD.md](./PRD.md)를 참고하세요.

## 프로젝트 개요
Claude 웹개발 수업의 필기를 정리하기 위한 **1인용 로컬 노트 웹앱**입니다.
서버/로그인 없이, 브라우저 `localStorage`만으로 동작하는 순수 정적 사이트입니다.

## 기술 스택 & 원칙
- **순수 HTML/CSS/JavaScript만 사용**. 프레임워크, 번들러, 외부 CDN 의존성 없음.
  → `index.html`을 더블클릭하거나 정적 서버로 열기만 하면 바로 동작해야 함.
- 데이터 저장은 전부 `localStorage` 키 `"claude-class-notes:v1"` 하나에 JSON으로 저장.
  스키마는 PRD의 "정보 구조" 섹션을 따른다 (`Lecture`, `Note`).
- 새 의존성(라이브러리, 폰트 CDN 등)을 추가하기 전에 반드시 필요성을 재확인할 것 —
  이 프로젝트는 "오프라인에서도 완전히 동작"을 원칙으로 한다.

## 디자인 원칙 (매우 중요)
이 프로젝트의 시각 품질 기준은 **apple.com 수준**이다. 다음을 지킬 것:
- 넉넉한 여백과 그리드, 과하지 않은 그림자, 절제된 포인트 컬러(1~2개)
- 시스템 폰트 스택 사용 (`-apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI", ...`)
- 인터랙션에는 부드러운 transition (150~250ms, ease 계열) — 버튼/카드 hover, 화면 전환 등
- 라이트/다크 모드 모두 지원 (`prefers-color-scheme` 기반 + 수동 토글)
- 텍스트 대비, 클릭 영역 크기 등 접근성 기본기를 지킬 것
- 장식보다 정보 위계(hierarchy)를 우선한다 — 타이포그래피 크기/굵기로 위계를 표현

## 파일 구조 (예정)
```
/
├── PRD.md
├── CLAUDE.md
├── index.html      # 앱 진입점 (마크업 전체)
├── styles.css       # 전체 스타일
└── app.js           # 상태 관리, localStorage 연동, 렌더링 로직
```
모든 로직을 하나의 파일에 몰아넣지 말고 위 3개 파일로 관심사를 분리한다.
(빌드 과정이 없으므로 파일을 과도하게 쪼개지는 않는다 — 이 정도 규모면 3파일이 적정선)

## 작업 시 유의사항
- **데이터 손실 방지**: `localStorage` 관련 로직을 수정할 때는 기존 저장 데이터의
  마이그레이션(또는 최소한 하위 호환)을 고려한다. 무조건 덮어쓰지 않는다.
- **자동 저장**: 사용자가 별도 "저장" 버튼을 누르지 않아도 입력이 끊기면(디바운스)
  자동으로 저장되어야 한다. 이것이 이 앱의 핵심 UX다.
- **내보내기/가져오기**: 데이터 백업 수단이 `localStorage` 하나뿐이므로, JSON
  export/import 기능은 절대 깨지면 안 되는 핵심 기능으로 취급한다.
- 새 기능을 추가할 때 PRD의 "비목표" 범위(로그인, 서버 DB, 실시간 협업 등)를
  벗어나지 않는지 확인한다. 필요하다고 판단되면 먼저 사용자에게 확인한다.
- UI 변경 후에는 실제 브라우저에서 라이트/다크 모드, 데스크톱/모바일 폭을 모두
  확인한다.

## 커밋 관련
이 저장소는 아직 별도 git 초기화가 되어있지 않을 수 있다. 사용자가 명시적으로
요청하기 전에는 git init/commit을 임의로 수행하지 않는다.
