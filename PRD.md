# PRD — Claude 웹개발 수업 노트 정리 앱

## 1. 배경 & 문제 정의
사용자는 Claude를 활용한 웹 개발 수업을 수강 중이다. 수업 진도와 정보량이 많아
필기 내용을 그때그때 기억하기 어렵고, 흩어진 메모(노트 앱, 종이, 캡처 등)로는
나중에 다시 찾아보기 힘들다. 수업 내용을 구조적으로 기록하고, 브라우저를 열기만
하면 언제든 다시 꺼내볼 수 있는 "나만의 수업 노트 허브"가 필요하다.

## 2. 목표
- 수업 중/후에 빠르게 노트를 작성·정리할 수 있다.
- 작성한 노트는 브라우저에 자동 저장되어 껐다 켜도 사라지지 않는다 (별도 서버/로그인 불필요).
- 여러 회차(Lecture)의 노트를 한눈에 훑어보고, 검색·필터로 원하는 내용을 빠르게 찾는다.
- 필기를 백업하거나 다른 기기로 옮길 수 있도록 내보내기/가져오기를 지원한다.
- 시각적으로 Apple.com 수준의 절제되고 세련된 UI/UX를 제공해, 매일 열어도 기분 좋게 쓸 수 있는 도구가 된다.

## 3. 비목표 (Out of Scope)
- 다중 사용자 로그인/계정 시스템, 서버 DB 연동 (1인용 로컬 저장 앱)
- 실시간 협업, 클라우드 동기화 (추후 확장 여지는 남기되 1차 범위 아님)
- 모바일 네이티브 앱 (반응형 웹으로 모바일 브라우저 대응은 포함)

## 4. 타겟 사용자
- 본인(수강생) 1인. Claude/웹개발 수업을 들으며 매 회차 노트를 남기고 복습하는 용도.

## 5. 핵심 기능 (Functional Requirements)

### 5.1 노트 CRUD
- 노트 생성: 제목, 본문(마크다운 유사 서식 지원 — 굵게/목록/코드블럭 등 최소한), 태그, 회차(강의 회차) 선택
- 노트 수정 / 삭제 (삭제 시 확인 절차)
- 노트는 최종 수정 시각 기준 자동 정렬

### 5.2 강의 회차(Lecture) 관리
- 노트를 "1강 - 프롬프트 기초" 같은 회차 단위로 그룹핑
- 회차 목록을 사이드바 또는 상단에서 전환하며 볼 수 있음
- 회차 추가/이름 변경/삭제

### 5.3 검색 & 필터
- 제목/본문/태그 통합 검색 (입력 즉시 필터링)
- 태그 클릭으로 필터링
- 회차별 보기 전환

### 5.4 로컬 영속 저장 ("메모장처럼 언제든 사용 가능")
- 모든 데이터는 브라우저 `localStorage`에 자동 저장 (입력 후 디바운스로 자동 저장, 별도 저장 버튼 불필요)
- 페이지를 새로고침/재접속해도 데이터 유지
- **내보내기(Export)**: 전체 노트를 JSON 파일로 다운로드 (백업/이관용)
- **가져오기(Import)**: JSON 파일을 업로드해 노트 복원

### 5.5 대시보드 / 홈 화면
- 최근 수정한 노트, 전체 노트 수, 회차 수 등 요약 정보
- 빈 상태(첫 사용) 시 친절한 온보딩 안내

## 6. 비기능 요구사항 (Non-Functional)
- **디자인 퀄리티**: apple.com 수준 — 넉넉한 여백, 절제된 색상(뉴트럴 베이스 + 포인트 컬러 1~2개), 시스템 폰트 기반 타이포그래피, 부드러운 트랜지션/마이크로 인터랙션, 다크모드 지원
- **반응형**: 데스크톱/태블릿/모바일 모두 자연스럽게 동작
- **성능**: 순수 정적 페이지(HTML/CSS/JS), 별도 빌드 과정 없이 브라우저에서 바로 실행 (`index.html` 더블클릭으로도 구동)
- **접근성**: 키보드로 주요 동작(새 노트, 검색 포커스 등) 가능, 명확한 대비의 색상
- **의존성 최소화**: 외부 CDN/프레임워크 없이 순수 HTML/CSS/JS로 구현 (오프라인에서도 완전히 동작)

## 7. 정보 구조 (데이터 모델)
```
Lecture {
  id: string
  title: string        // 예: "1강. 프롬프트 엔지니어링 기초"
  order: number
  createdAt: number
}

Note {
  id: string
  lectureId: string | null   // 미분류 가능
  title: string
  content: string             // plain text + 간단 서식(줄바꿈, 목록 등)
  tags: string[]
  createdAt: number
  updatedAt: number
}
```
저장 위치: `localStorage["claude-class-notes:v1"]` (Lecture[], Note[] 포함한 단일 JSON)

## 8. 사용자 흐름 (User Flow)
1. 페이지 접속 → 대시보드(최근 노트 요약) 표시
2. "+ 새 노트" 클릭 → 회차 선택(또는 새 회차 생성) → 제목/본문/태그 작성 → 자동 저장
3. 사이드바에서 회차 클릭 → 해당 회차 노트 목록만 표시
4. 검색창에 키워드 입력 → 실시간 필터링된 결과 표시
5. 노트 클릭 → 상세/편집 뷰로 전환, 수정 시 자동 저장
6. 설정/메뉴에서 "내보내기" → JSON 다운로드로 백업
7. 새 기기/브라우저에서 "가져오기"로 백업 복원

## 9. 성공 기준 (Acceptance Criteria)
- [ ] 노트 생성 → 새로고침 후에도 데이터가 그대로 남아있다
- [ ] 회차별로 노트를 분류하고 전환할 수 있다
- [ ] 검색어 입력 시 관련 노트만 즉시 필터링된다
- [ ] JSON 내보내기/가져오기가 정상 동작한다
- [ ] 데스크톱/모바일 화면 모두에서 레이아웃이 깨지지 않는다
- [ ] 다크모드 전환 시 모든 요소가 자연스럽게 대응한다
- [ ] 디자인이 여백/타이포/컬러 면에서 절제되고 세련되어 있다 (apple.com 톤앤매너)

## 10. 향후 확장 아이디어 (참고용, 이번 범위 아님)
- 클라우드 동기화 / 여러 기기 간 공유
- 마크다운 미리보기, 이미지 첨부
- Claude API 연동 노트 요약/정리 자동화
